# Experiment Configuration Example
robot:
  model: diff_drive
  sensors: [camera]
simulation:
  scene: default.ttt
perception:
  task: semantic_segmentation
  model: custom_cnn
planning:
  algorithm: astar
control:
  algorithm: ppo
training:
  epochs: 100
  batch_size: 32
  lr: 0.0003
